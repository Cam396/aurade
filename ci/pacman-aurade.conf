# Example pacman repository stanza for the AuraDE soak repo.
#
# Replace the Server value with the published HTTPS URL or a local file:// path.
# For signed repos, import and locally sign the AuraDE packaging key before use.

[aurade]
SigLevel = Required DatabaseOptional
Server = https://repo.example.invalid/stable/x86_64
