# Build artifacts of the screenshot loop. build_v3.py writes these beside
# itself; ci/build-files-page.sh writes the shipped pair into an output
# directory instead and leaves nothing here.
v3.html
site/
site-light/
__pycache__/

# verify_all.py writes this while proving the preview runs under the page's
# Trusted Types policy. Output, like v3.html, not a source.
v3-tt.html
