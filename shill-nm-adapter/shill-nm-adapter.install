post_install() {
    systemctl daemon-reload >/dev/null 2>&1 || true
    systemctl enable shill-nm-adapter.service >/dev/null 2>&1 || true
}

post_upgrade() {
    post_install
}

pre_remove() {
    systemctl disable --now shill-nm-adapter.service >/dev/null 2>&1 || true
    systemctl daemon-reload >/dev/null 2>&1 || true
}
